# 🚀 Start Frontend Command

## Quick Start

```bash
cd "/Users/saisharan.v/Desktop/new project/frontend"
npm run dev
```

## Full Setup (if dependencies not installed)

```bash
# Step 1: Navigate to frontend directory
cd "/Users/saisharan.v/Desktop/new project/frontend"

# Step 2: Install dependencies (first time only)
npm install

# Step 3: Start development server
npm run dev
```

## One-Liner

```bash
cd "/Users/saisharan.v/Desktop/new project/frontend" && npm run dev
```

## Available Scripts

- `npm run dev` - Start development server (with hot reload)
- `npm run build` - Build for production
- `npm run preview` - Preview production build

## Default Port

The frontend will typically run on: **http://localhost:5173** (Vite default)

## Notes

- Make sure Node.js and npm are installed
- The frontend uses Vite + React + TypeScript + Tailwind CSS
- Hot module replacement (HMR) is enabled for fast development

